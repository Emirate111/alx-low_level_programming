#include "main.h"

/**
 * print_last_digit - prints the last digit
 * @n: the number
 * Return: value if the last digit
 */
int print_last_digit(int n)
{
	int ld = % 10;

	if (ld < 0)
		ld *= -1;

	_putchar(la + '0');

	return (0);
}
